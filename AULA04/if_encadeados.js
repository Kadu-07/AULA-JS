let media = 6
let faltas = 2

if (media >= 7 && faltas <= 5 ) {
    console.log(`Jovem APROVADO com média: ${media} e faltas: ${faltas}`);

} else if (media >= 6 && faltas <= 5) {
    console.log(`Jovem Ap. Conselho com média: ${media} e faltas: ${faltas}`);

} else {
    console.log(`Jovem REPROVADO com média: ${media} e faltas: ${faltas}`);
    
}