let media = 8
let faltas = 4

if (media >= 7 && faltas <= 5) {
    console.log(`Jovem APROVADO com média: ${media} e faltas: ${faltas}`);
    
} else {
    console.log(`Jovem REPROVADO com média: ${media} e faltas: ${faltas}`);
    
}